def compute_metrics(explanations):
    # Compute coverage, fidelity, and comprehensibility metrics
    # ...

    metrics = {
        "coverage": coverage,
        "fidelity": fidelity,
        "comprehensibility": comprehensibility
    }

    return metrics
